<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tyg.dao.IUserDao">

    <insert id="addUser" parameterType="User" useGeneratedKeys="true" keyProperty="user_name">
        insert into user(user_name,password,email,telephone) values (#{user_name},#{password},#{email},#{telephone});
    </insert>

    <update id="updateUser">
        update user
        <set>
            <if test="user_name != null">
                user_name = #{user_name},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
        </set>
        where user_name = #{user_name}
    </update>

    <delete id="deleteUser">
        delete from user where user_name = #{user_name}
    </delete>

    <select id="getUsers" resultType="User">
        select * from user
    </select>

    <select id="getUserByName" resultType="User">
        select * from user where user_name = #{user_name}
    </select>
</mapper>